
This directory contains tests to be run with 

    gcc <filename> && ./a.out

The tests are self contained to test functions and code rather than test the final codebase as this allows a
much faster cycle time than running on the target MCU, especially if the target MCU is not available.